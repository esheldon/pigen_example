modulename: _pywrap

includes:
    - '#include "vec.h"'

functions:
    - def: double vec_dot(const struct vec* v1, const struct vec* v2)
      doc: dot product of two vectors
    - def: void vec_print(const struct vec* self, const char *extra);
      doc: print a representation
